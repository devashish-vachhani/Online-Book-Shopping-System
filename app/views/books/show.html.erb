<p style="color: green"><%= notice %></p>
<div class="card">
  <div class="card-body">
    <%= render @book %>
      <%= link_to "Edit", edit_book_path(@book), class: 'btn btn-primary' %>
      <br/>
      <br/>
      <%= button_to "Destroy", @book, data: { turbo_confirm: "Are you sure?" }, method: :delete, class: 'btn btn-danger' %>
    <br/>
    <%= link_to "Back to books", books_path %>
  </div>
</div>