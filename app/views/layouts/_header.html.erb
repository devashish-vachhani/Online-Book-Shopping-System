<nav class="navbar navbar-expand-lg navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <% if admin_signed_in? || user_signed_in? %>
      <%= link_to 'Dashboard', authenticated_root_path, class:'navbar-brand'%>
    <% else %>
      <%= link_to 'Online Book Shopping', root_path, class:'navbar-brand'%>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if admin_signed_in? || user_signed_in? %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item me-auto"><a class="nav-link">Signed in as: <%= user_signed_in? ? current_user.username : current_admin.username %></a></li>
        </ul>
        <ul class="navbar-nav ml-auto">
          <% if user_signed_in? %><li class="nav-item"><%= link_to "Cart", shopping_cart_path, class: "nav-link active" %></li><% end %>
          <li class="nav-item"><%= link_to "Edit Profile", (user_signed_in? ? edit_user_registration_path : edit_admin_registration_path), class: "nav-link active" %></li>
          <li class="nav-item"><%= link_to "Sign out", (user_signed_in? ? destroy_user_session_path : destroy_admin_session_path), 'data-turbo-method': :delete, class: "nav-link active" %></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
